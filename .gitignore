# ── Poids entraînés (volumineux, régénérables par training/) ─────────────
weights/
**/weights/
archive/weights_massive_gap_diag/

# ── Caches de canaux Sionna (générés dans /export/tmp, cf. README) ───────
*.npz
**/*.npz

# ── Logs, bytecode ──────────────────────────────────────────────────────
logs/
**/logs/
__pycache__/
*.pyc

# ── Sorties brutes legacy (non canoniques, cf. archive/README.md) ────────
archive/legacy/results/*.png
archive/legacy/results/*.npy
